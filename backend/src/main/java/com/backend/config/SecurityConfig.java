package com.backend.config;

public class SecurityConfig {
    // Purpose: Configures security for your application, including JWT authentication and which routes are publicly accessible (e.g., login/signup) and which require authentication (e.g., creating a route).
    // Interactions: It interacts with the JwtRequestFilter to validate JWT tokens, and also configures Spring Security to handle authentication and authorization.
}
